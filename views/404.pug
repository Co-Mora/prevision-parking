extends layout

block content
     div.middle-box.text-center.animated.fadeInDown
        h1 404
        h3.font-bold Page Not Found
        div.error-desc Sorry, but the page you are looking for has note been found. Try checking the URL for error, then hit the refresh button on your browser or try found something else in our app.
        a.btn.btn-primary(href="/" style="margin-top: 15px") Go Back